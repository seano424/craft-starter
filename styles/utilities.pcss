/* Custom utilities */

@layer utilities {
	/* Use my-base to add spacing between modules */
	.my-base {
		@apply my-6 sm:my-10 md:my-14;
	}

	/* Use py-base to add spacing within a module (like when using a background color) */
	.py-base {
		@apply py-6 sm:py-10 md:py-14;
	}
}
